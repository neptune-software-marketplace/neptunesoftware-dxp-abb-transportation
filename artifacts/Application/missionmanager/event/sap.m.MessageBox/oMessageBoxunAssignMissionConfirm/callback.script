if (oEvent === "OK"){
    var data = modeloPageMissionDetails.getData();
    
    var options = {
        data: {
            "id": data.id
        }
    };

    apioRestAPIunAssignMission(options);
    
}


